/**
 * NOT A LIVE TEST — pointer stub.
 *
 * The real regression test is src/nginx-brand-assets.test.ts (vitest's
 * include pattern is src/**, so a test at the repo root never runs — the
 * same reason src/nginx-csp.test.ts and src/deploy-workflow.test.ts live
 * under src/).
 *
 * This file was created at the root by mistake on 2026-08-24 and is left in
 * place deliberately: repo policy is that the assistant never deletes files.
 * It is safe to remove by hand whenever convenient.
 */
export {};
