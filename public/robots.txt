User-agent: *
Allow: /
Disallow: /admin
Disallow: /dashboard
Disallow: /api/
Sitemap: https://parametric-memory.dev/sitemap.xml

# AI agent discovery — see https://llmstxt.org
# Provides structured context for LLMs, AI coding assistants, and agents
# llms.txt: https://parametric-memory.dev/llms.txt
#
# ── High-value pages for AI answer engines (AEO) ──────────────────────────
# faq: https://parametric-memory.dev/faq
#   FAQPage JSON-LD schema. Covers competitor comparisons (vs Mem0, Zep),
#   Merkle proof verification, Markov prediction, agent benefits, and pricing.
#   Optimised for citation by AI answer engines (Google AI Mode, Perplexity,
#   ChatGPT). Full sitemap: https://parametric-memory.dev/sitemap.xml

# ══════════════════════════════════════════════════════════════════════════
# AI bots — 2026 taxonomy (updated per AI-FIRST-DISTRIBUTION-PLAYBOOK.md)
# Each engine now runs SEPARATE bots for (1) search/citation, (2) live user
# fetch, and (3) model training. Being CITED requires allowing the search +
# user-fetch bots. Allowing/blocking the TRAINING bots does NOT affect
# citations — that is purely an IP/content-licensing choice.
# ══════════════════════════════════════════════════════════════════════════

# ── 1. Search / citation crawlers — ALLOW (these are what get you cited) ───
# These index your pages so the AI answer surfaces can quote and link them.
# Your users are AI users, so being indexed here is pure marketing.

User-agent: Googlebot
Allow: /
# ↑ Also powers Google AI Overviews & AI Mode — you cannot be in Google AI
#   answers without allowing Googlebot. Mandatory.

User-agent: Bingbot
Allow: /
# ↑ ChatGPT Search sources ~87% of its citations from the Bing index — this
#   is the de-facto path to being cited in ChatGPT.

User-agent: OAI-SearchBot
Allow: /
# ↑ OpenAI's SEARCH/citation crawler for ChatGPT Search (distinct from the
#   GPTBot training crawler below). Previously MISSING — this is the ChatGPT
#   citation path and the single most important addition here.

User-agent: Claude-SearchBot
Allow: /
# ↑ Anthropic's search/citation crawler that powers Claude's web citations
#   (distinct from ClaudeBot training + Claude-User live fetch).

User-agent: PerplexityBot
Allow: /

User-agent: cohere-ai
Allow: /

User-agent: YouBot
Allow: /

User-agent: DuckAssistBot
Allow: /
# ↑ DuckDuckGo AI Chat (Duck.ai).

# ── 2. Live user-fetch agents — ALLOW (fetch a page when a user asks) ──────
# Triggered by a user action; high-intent, always worth allowing.

User-agent: ChatGPT-User
Allow: /

User-agent: Claude-User
Allow: /

User-agent: Perplexity-User
Allow: /

User-agent: MistralAI-User
Allow: /
# ↑ Mistral / Le Chat — growing EU citation surface.

User-agent: Meta-ExternalAgent
Allow: /
# ↑ Meta AI — answers in WhatsApp, Instagram, Messenger.

# ── 3. Model-training crawlers — ALLOW by default (IP CHOICE, not citation) ─
# NOTE: blocking any bot in THIS section costs you ZERO citations — the
# search/user bots above are what get you quoted. Left as Allow because the
# AI-first strategy favours maximum presence (incl. in model weights). To
# protect blog/docs from training, change these to `Disallow: /` (or scope to
# `Disallow: /blog`) with no loss of AI-search visibility.

User-agent: GPTBot
Allow: /

User-agent: ClaudeBot
Allow: /

User-agent: anthropic-ai
Allow: /

User-agent: Google-Extended
Allow: /

User-agent: Applebot-Extended
Allow: /
# ↑ Apple Intelligence training signal.

# ── 4. Bulk dataset scrapers — BLOCK (no referral value) ───────────────────

User-agent: CCBot
Disallow: /

User-agent: CommonCrawlBot
Disallow: /
